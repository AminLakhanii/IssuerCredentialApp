import '../pages/global.css';

// Rest of the _app.js code
function MyApp({ Component, pageProps }) {
  // Add any necessary global context or providers

  return <Component {...pageProps} />;
}

export default MyApp;
